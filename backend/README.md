# Backend

This directory is a placeholder for the Sleep Naked backend API.

The backend will be implemented using Node.js / Express and TypeScript. See `docs/architecture.md` for the planned technology stack and API design.

## Structure (planned)

```
backend/
├── src/
│   ├── routes/
│   ├── controllers/
│   ├── services/
│   ├── models/
│   ├── middleware/
│   └── utils/
├── prisma/
│   └── schema.prisma
├── package.json
├── tsconfig.json
└── Dockerfile
```
